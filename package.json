{
  "name": "storik",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @storik/desktop dev",
    "dev:desktop": "pnpm --filter @storik/desktop dev",
    "dev:api": "pnpm --filter @storik/api dev",
    "dev:web": "pnpm --filter @storik/web dev",
    "build": "pnpm --filter @storik/desktop build",
    "build:web": "pnpm --filter @storik/web build",
    "build:desktop": "pnpm --filter @storik/desktop build",
    "test:desktop": "pnpm --filter @storik/desktop test:rust",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "pnpm --filter e2e exec playwright test",
    "test:e2e:ui": "pnpm --filter e2e exec playwright test --ui"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "vitest": "^4.0.18"
  }
}
